const [a, b, c, d] = [3, 5, 1, 15];

const soma = a + b + c + d;//operador binário
const subtracao = d - b;
const multiplicacao = a * b;
const divisao = d / a;
const modulo = a % 2;

console.log(soma, subtracao, multiplicacao, divisao, modulo);

//-4 - operador unário
//d++ - postfixed
//++d - prefixed
//d + d - infixed