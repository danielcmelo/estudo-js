const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado';//armazenando o valor em uma função arrow
//nota >= 7 ? 'Aprovado' : 'Reprovado' -> Esse é um operador ternário

console.log(resultado(5.1));

const status = nota >= 7 ? 'Aprovado' : 'Reprovado';//armazenando o valor em uma constante